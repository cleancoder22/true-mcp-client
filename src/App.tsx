import type { Component } from 'solid-js';

import logo from './logo.svg';

const App: Component = () => {
  return (
    <div class="min-h-screen bg-gray-100 flex items-center justify-center">
      <div class="text-center">
        <h1 class="text-4xl font-bold text-gray-800 mb-2">Welcome to SolidJS with Tailwind CSS!</h1>
        <p class="text-gray-600">This is a simple starter template.</p>
      </div>
    </div>
  );
};

export default App;
